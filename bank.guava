epicarp {
    universal int saldo = 0;
    universal int x = 2;
    branch {
        lock(saldo) {
            saldo = saldo + 20;
        }
    }
    lock(saldo) {
        saldo = saldo + 10;
    }
    branch {
        int s = 2;
        lock(saldo) {
            int k = 69;
            for (int i = 0; i < 8; i++) {
                saldo = saldo - 10;
            }
        }
    }
}